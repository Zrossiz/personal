import { Layout } from "@/layout/Layout"

const GoRecommendationEngine = () => {
    return (
        <Layout>
            <div>go recommendation engine page</div>
        </Layout>
    )
}

export default GoRecommendationEngine;